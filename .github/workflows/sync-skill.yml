name: Sync SKILL.md and rules

on:
  push:
    branches: [main]
    paths:
      - skills/cavemaenggu/SKILL.md
      - rules/cavemaenggu-activate.md
      - cavemaenggu-compress/SKILL.md
      - cavemaenggu-compress/scripts/**

concurrency:
  group: sync-skill
  cancel-in-progress: false

jobs:
  sync:
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - uses: actions/checkout@v4
        with:
          ref: main

      - name: Pull latest before making changes
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git pull --rebase origin main

      - name: Sync SKILL.md copies
        run: |
          cp skills/cavemaenggu/SKILL.md cavemaenggu/SKILL.md
          cp skills/cavemaenggu/SKILL.md plugins/cavemaenggu/skills/cavemaenggu/SKILL.md
          cp skills/cavemaenggu/SKILL.md .cursor/skills/cavemaenggu/SKILL.md
          mkdir -p .windsurf/skills/cavemaenggu
          cp skills/cavemaenggu/SKILL.md .windsurf/skills/cavemaenggu/SKILL.md

      - name: Sync compress skill
        run: |
          # Sync to skills/compress (main location for Gemini CLI)
          rm -rf skills/compress/scripts
          cp cavemaenggu-compress/SKILL.md skills/compress/SKILL.md
          sed -i 's/^name: cavemaenggu-compress$/name: compress/' skills/compress/SKILL.md
          sed -i "s|The compression scripts live in \`cavemaenggu-compress/scripts/\` (adjacent to this SKILL.md). If the path is not immediately available, search for \`cavemaenggu-compress/scripts/__main__.py\`.|This SKILL.md lives alongside \`scripts/\` in the same directory. Find that directory.|" skills/compress/SKILL.md
          sed -i 's|cd cavemaenggu-compress && python3|cd <directory_containing_this_SKILL.md> \&\& python3|' skills/compress/SKILL.md
          cp -r cavemaenggu-compress/scripts skills/compress/scripts
          rm -rf skills/compress/scripts/__pycache__

      - name: Sync compress skill to plugin
        run: |
          # Copy SKILL.md and patch name + process instructions for plugin context
          cp cavemaenggu-compress/SKILL.md plugins/cavemaenggu/skills/compress/SKILL.md
          sed -i 's/^name: cavemaenggu-compress$/name: compress/' plugins/cavemaenggu/skills/compress/SKILL.md
          sed -i "s|The compression scripts live in \`cavemaenggu-compress/scripts/\` (adjacent to this SKILL.md). If the path is not immediately available, search for \`cavemaenggu-compress/scripts/__main__.py\`.|This SKILL.md lives alongside \`scripts/\` in the same directory. Find that directory.|" plugins/cavemaenggu/skills/compress/SKILL.md
          sed -i 's|cd cavemaenggu-compress && python3|cd <directory_containing_this_SKILL.md> \&\& python3|' plugins/cavemaenggu/skills/compress/SKILL.md
          # Copy scripts verbatim
          rm -rf plugins/cavemaenggu/skills/compress/scripts
          cp -r cavemaenggu-compress/scripts plugins/cavemaenggu/skills/compress/scripts
          rm -rf plugins/cavemaenggu/skills/compress/scripts/__pycache__

      - name: Rebuild cavemaenggu.skill ZIP
        run: cd skills && zip -r ../cavemaenggu.skill cavemaenggu/

      - name: Sync auto-activation rules
        run: |
          BODY="rules/cavemaenggu-activate.md"

          # Cline + Copilot — no frontmatter, direct copy
          mkdir -p .clinerules .github
          cp "$BODY" .clinerules/cavemaenggu.md
          cp "$BODY" .github/copilot-instructions.md

          # Cursor — needs alwaysApply frontmatter
          mkdir -p .cursor/rules
          printf '%s\n' '---' 'description: "Cavemaenggu mode — terse communication (caveman + 맹구 fork), ~75% fewer tokens, full technical accuracy"' 'alwaysApply: true' '---' '' > .cursor/rules/cavemaenggu.mdc
          cat "$BODY" >> .cursor/rules/cavemaenggu.mdc

          # Windsurf — needs always_on frontmatter
          mkdir -p .windsurf/rules
          printf '%s\n' '---' 'trigger: always_on' '---' '' > .windsurf/rules/cavemaenggu.md
          cat "$BODY" >> .windsurf/rules/cavemaenggu.md

      - name: Commit and push if changed
        run: |
          git add \
            cavemaenggu/SKILL.md \
            skills/compress/ \
            plugins/cavemaenggu/skills/compress/ \
            plugins/cavemaenggu/skills/cavemaenggu/SKILL.md \
            .cursor/skills/cavemaenggu/SKILL.md \
            .windsurf/skills/cavemaenggu/SKILL.md \
            cavemaenggu.skill \
            .clinerules/cavemaenggu.md \
            .github/copilot-instructions.md \
            .cursor/rules/cavemaenggu.mdc \
            .windsurf/rules/cavemaenggu.md
          git diff --staged --quiet && exit 0
          git commit -m "chore: sync SKILL.md copies and auto-activation rules [skip ci]"
          git push
